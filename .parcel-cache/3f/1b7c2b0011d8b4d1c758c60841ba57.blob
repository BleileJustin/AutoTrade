module.exports = exports = {
  getExchangeChoice: () => {
    const exchangeDropdownChoice = document.getElementById("exchange-dropdown").value;
    console.log(exchangeDropdownChoice);
    return exchangeDropdownChoice;
  },
  checkExchangeDropdown: () => {
    const exchangeDropdownValue = document.getElementById("exchange-dropdown").value;
    if (exchangeDropdownValue == 0) {
      return false;
    } else {
      return true;
    }
  },
  getStrategyChoice: () => {
    const strategyDropdownChoice = document.getElementById("strategy-dropdown").value;
    console.log(strategyDropdownChoice);
    return strategyDropdownChoice;
  },
  checkStrategyDropdown: () => {
    const strategyDropdownValue = document.getElementById("strategy-dropdown").value;
    if (strategyDropdownValue == 0) {
      return false;
    } else {
      return true;
    }
  }
};
